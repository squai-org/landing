<section class="section search-intent" aria-labelledby="search-intent-title">
  <div class="shell">
    <div class="search-intent-heading">
      <p class="search-intent-eyebrow">Formación en inteligencia artificial</p>
      <h2 id="search-intent-title" class="display d-h1">
        Aprende, integra y adopta inteligencia artificial.
      </h2>
      <p class="lead search-intent-intro">
        No necesitas programar ni hablar en tecnicismos. Empiezas por entender cómo funciona la
        inteligencia artificial y avanzas hasta usarla en tu vida, tu trabajo o tu
        comunidad educativa.
      </p>
    </div>

    <div class="search-intent-grid">
      <article class="search-intent-card">
        <h3 class="display d-h3">Aprender IA potencia tus habilidades</h3>
        <p class="body">
          Un programa en vivo para comprender la IA, practicar con herramientas como
          ChatGPT, Gemini y Claude, y desarrollar habilidades que siguen siendo útiles cuando la
          tecnología cambia.
        </p>
        <a href="/servicios/squai-one" class="search-intent-link">
          Conocer el programa para personas <span aria-hidden="true">→</span>
        </a>
      </article>

      <article class="search-intent-card">
        <h3 class="display d-h3">Entrenar equipos e integrar IA</h3>
        <p class="body">
          Entrenamiento en inteligencia artificial para empresas que quieren llevar la IA a tareas
          y procesos reales, con acuerdos de uso seguro y una adopción que no termine después del
          primer piloto.
        </p>
        <a href="/servicios/squai-grow" class="search-intent-link">
          Ver capacitación para empresas <span aria-hidden="true">→</span>
        </a>
      </article>

      <article class="search-intent-card">
        <h3 class="display d-h3">Formar comunidades educativas</h3>
        <p class="body">
          Formación en IA para docentes, estudiantes, colegios y universidades que buscan adoptar
          estas herramientas con ética y pensamiento crítico para aprender y
          enseñar.
        </p>
        <a href="/servicios/squai-learn" class="search-intent-link">
          Explorar formación para comunidades educativas<span aria-hidden="true">→</span>
        </a>
      </article>
    </div>
  </div>
</section>

<style>
  .search-intent { background: var(--canvas); }
  .search-intent-heading { max-width: 880px; }
  .search-intent-eyebrow {
    margin: 0 0 14px;
    color: var(--periwinkle-ink);
    font-size: 14px;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .search-intent-heading h2 { color: var(--ink); }
  .search-intent-intro { max-width: 72ch; margin-top: 22px; color: var(--ink-body); }
  .search-intent-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(14px, 2vw, 24px);
    margin-top: clamp(36px, 4vw, 64px);
  }
  .search-intent-card {
    display: flex;
    min-width: 0;
    padding: clamp(24px, 3vw, 40px);
    flex-direction: column;
    gap: 18px;
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    background: var(--surface);
  }
  .search-intent-card h3 { color: var(--ink); }
  .search-intent-card p { color: var(--ink-body); }
  .search-intent-link {
    display: inline-flex;
    margin-top: auto;
    align-items: center;
    gap: 8px;
    color: var(--periwinkle-ink);
    font-weight: 700;
  }
  .search-intent-link:hover { color: var(--ink); }

  @media (max-width: 900px) {
    .search-intent-grid { grid-template-columns: 1fr; }
  }
</style>
